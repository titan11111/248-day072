# 塩見の館 — 仕様

## 0. ドキュメント情報
- 対象ゲーム: 248-shiomi-no-yakata（GitHubリポジトリ名・公開URLは `248-day072` のまま。2026-09-21改名時に旧URL維持を選択）
- 作成日: 2026-09-19
- 更新日: 2026-09-22（操作盤をファミコン準拠へ全面改訂）
- ステータス: 実装済み
- 参照ファイル: index.html / style.css / script.js / images/

### ⚠️ 別名・混同注意（2026-09-22 追加）
この作品は**3つの名前**を持ち、さらに近隣に似た名前が2つある。指示が別フォルダへ流れる事故が実際に起きた。

| 種別 | 名前 |
|---|---|
| フォルダ名 | `248-shiomi-no-yakata` |
| リポジトリ名・公開URL | `248-day072`（404回避のため据え置き） |
| 作品タイトル | 塩見の館（しおみのやかた） |
| 音声入力での誤記例 | 「COMの館」「シオミの館」 |

**紛らわしい別作品（手を触れないこと）**
- `243-shiosai-no-yoru`（潮騒の夜）＝**推理サウンドノベル**。妖怪も戦闘も動画も無い別ジャンル
- `247-shiomiso`

妖怪・女将・お茶・提灯・ステージの話は**すべてこの 248 のこと**。

## 1. ゲーム概要
- ジャンル: 部屋送りアクション（会話→移動→斬撃）
- 一言説明: 夜の宿に招かれ、おもてなしの正体を斬る。
- **目的（2026-09-22 確定）: この島を、夜から剥がして帰る。** 塩を四つ集めて船の舳先から撒く
- **女将撃破はゴールではなく引き金**。女将は主ではなく戸口に立つ門番で、斬った瞬間に館が目を覚まし島ごと夜の側へ引き込まれる。目的が「勝つ」から「帰る」へ反転する
- 想定プレイ時間: 5〜8分
- 想定プレイヤー: iPhone縦持ち。100日チャレンジ Day072
- クリア体験の要点: 女将の歓迎から提灯お化け、中庭、正体までを一続きで斬り抜ける
- 戦闘カメラ: プレイ中は背景をキャラスプライト（420px高）と同倍率で描き、床線を `gy()` に合わせる。会話は室内を cover、屋外は従来どおり立ち絵

## 2. 対象環境

### 必須
- 配信先: GitHub Pages
- 最優先端末: iPhone Safari
- 対応画面幅: 320px〜430px
- 実装方式: 静的 HTML / CSS / JS。外部CDNなし

## 3. 操作 / 設定UI（必須・実装1手目）

**2026-09-22 全面改訂: 操作盤をファミコン実機準拠のコントローラにした。**

| 項目 | 内容 |
|---|---|
| 画面 | 上75% `#game-stage` / 下25% `#control-deck` |
| 操作盤の見た目 | エンジの本体 + シャンパンゴールドのプレート。左=黒い十字キー / 中=黒い窪みのSELECT・START / 右=黒いハウジングの赤いA・B |
| ボタン配置 | 実機どおり **B が左・A が右**、**SELECT が左・START が右** |
| A | 会話送り・斬る・拾う（`#btnAct`）。状況名はボタン下のラベル `#lblAct` が示す（ボタン面の文字は「A」で固定） |
| B | 跳ぶ（`#btnJump`）。`mode!=='play'` のとき無効化し、ラベル「跳」も減光する |
| START | 一時停止（`#btnPause`） |
| SELECT | ミュート（`#btnMute`）。**`USE_AUDIO=false` の現在は `disabled`**。音を積み直したとき script 側が `disabled` を外して有効化する |
| 入力 | Pointer Events + `setPointerCapture`。十字はDOMオーバーレイ |
| キーボード | ←→↑↓ / Z・X 斬 / Space 跳または次へ / Enter 次へ / P・Escape ポーズ / M ミュート |
| ポーズ | START / P / Escape / タブ非表示。**ダイアログの「再開」「タイトルへ」は 2026-09-22 に初めて配線した**（それ以前は開くと閉じられなかった） |
| ミュート | `tg.248.mute` |
| クリア記録 | `tg.248.cleared` `tg.248.best`（残りハート） `tg.248.salt`（集めた塩の最大値 0〜4） |
| 音声unlock | はじめるタップで AudioContext と BGM unlock |
| iOS | viewport / safe-area / ダブルタップ防止。body=`manipulation`、canvas=`none` |

### DOM順と見た目の順が違う理由（触る前に読む）
`index.html` の操作盤は **A/B → START/SELECT → 十字** の順で書かれ、CSS の `row-reverse` で
見た目を **十字 → SELECT/START → B/A** に戻している。

理由は、自動検証（`_tools/game-harness.mjs`）が `document` 先頭の `button` をタップ対象にするため。
先頭が無効化されうるボタン（SELECT や B）だと「押せない操作盤」と判定されて FAIL する。
**DOM順を素直に並べ替えるときは、先頭が常に有効な主操作ボタンであることを確認すること。**

### 実測値（2026-09-22 / WebKit・DPR2）

| 幅 | 十字 | SELECT/START | A・B | 重なり | 横はみ出し | 48px未満 |
|---|---|---|---|---|---|---|
| 320 | 61px | 106px | 106px | 0 | 0 | なし |
| 375 | 71px | 112px | 109px | 0 | 0 | なし |
| 390 | 90px | 114px | 121px | 0 | 0 | なし |
| 430 | 92px | 116px | 121px | 0 | 0 | なし |

SELECT・START は実機どおり平たいゴムに見せつつ、当たり判定だけ 48px 角を確保している
（背景は透明にして、赤いゴムは `::before` で中央に描く）。

## 4. コアループ
- 聞く → 走る → 斬る、を部屋単位で繰り返す
- ハートは装備で 3〜4。0でその部屋から再戦（レベルと装備は残る）
- 帳場で茶を受ける／断る。断ると提灯が1体増える
- 経験値で Lv1〜5。レベルと武器で斬れ味が上がる
- 稀に金提灯（経験値90）。祠の塩札は経験値100が付く
- 拾い場: 押入れ（館の羽織）→ 武具の間（仕込み刀）→ 祠（塩札）
- **塩**: 全4ステージを貫く目的の軸。各ステージの終わりに1つ得る。`tg.248.salt` に最大値を保存

## 4-2. ステージ構成（2026-09-22 タイタン指示で確定・段階実装）

| # | 舞台 | 内容 | 得るもの | 状態 |
|---|---|---|---|---|
| 1 | 館 | 帳場〜正体〜awake | 塩・壱 | **実装済み（2026-09-22）** |
| 2 | **島の外** | 館を出たあとの逃走。分かれ道を選ぶ形にする（自由移動の探索にはしない） | 塩・弐 | 未実装 |
| 3 | **港** | 既存の斬撃エンジンを流用 | 塩・参 | 未実装 |
| 4 | **海でボートレース** | 唯一の新規システム。撒いて脱出 | 撒いて脱出 | 未実装 |

最終クリア条件（ステージ4実装時）: 塩4つで撒く＝TRUE／不足のまま乗船＝沖で呑まれる BAD。
**ステージ2を「自由移動の探索」にすると部屋送りエンジンと衝突するため、「分かれ道を選ぶ」に落とす。**

## 5. 画面 / 状態遷移
- boot（はじめる）→ title（館へ入る・操作説明・ベスト）→ talk／play → over または clear
- タイトル画面のビジュアルは `images/title.webp`（旧 OP 静止画と同素材）。オープニング動画は廃止（2026-09-22）
- 部屋: 帳場 → 客間 → 押入れ → 武具の間 → 廊下 → 祠 → 中庭 → 正体 → **awake（引き金の幕・塩壱）**

## 6. 勝敗
- 第一幕の終わり: 正体の女将を倒し、awake の幕を読み切る（「クリア」ではなく次幕への引き金）
- 会話幕・拾い場・戦闘のどの遷移も `goNext()` を通す。`next` が `ROOMS.length` 以上なら `win()`（会話幕を末尾に置いても落ちない）
- 敗北: ハート0。再戦でその戦闘部屋へ
- スコア: クリア時の残りハートをベストとして保存

## 7. 音声
- **2026-09-22 一旦無効**: `USE_AUDIO=false`。BGMファイル・`<audio>` タグ・WebAudio SE すべて停止。復帰は `script.js` 先頭フラグ + `audio/inn.m4a` + index の audio/ミュートボタン
- （復帰時）開始タップで AudioContext と BGM要素をunlock。`audio/inn.m4a` をループ（読めないときだけ矩形＋三角＋ノイズ）

## 8. ファイル構成
```
248-shiomi-no-yakata/
  index.html
  style.css
  script.js
  audio/（inn.m4a は一旦削除・USE_AUDIO=false）
  images/
    title.webp
    bg-desk.webp
    bg-guest.webp
    bg-hall.webp
    bg-yard.webp
    spr-hero.webp
    spr-hero-slash.webp
    spr-hero-front.webp
    spr-okami.webp
    spr-okami-front.webp
    spr-okami-red.webp
    spr-lantern.webp
    port-hero.webp
    port-okami.webp
  SPEC.md
  LEARNINGS.md
```

## 9. 実装制約
- 公開実体 20.00MB 以下（`.git` 除外）
- バニラJS。`requestAnimationFrame`
- 台詞は `textContent`。ユーザー入力を innerHTML に渡さない
- 1ファイル 1000行超で分割

## 10. 未確定事項
- ステージ2〜4は未実装。ステージ4（ボートレース）のみ既存システムの流用が効かない
- iPhone実機は未実施（自動検証のみ）
