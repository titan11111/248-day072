# 248-shiomi-no-yakata（旧 248-day072）LEARNINGS

## 2026-09-22 目的の軸を入れ、女将撃破を「引き金」に変えた（ステージ1手目）

### 指示がずれ続けていた原因は、実装でもプロンプトでもなくフォルダ違いだった
タイタンから「243-shiosai-no-yoru の COMの館を作り直したい」と来たが、**243 に該当要素は1つも無い**。
実測: 243 の全ファイルを grep して「妖怪」0件 /「化け物」0件 /「お茶」0件、動画ファイル0個。
243 は離島の宿の**推理サウンドノベル**（5エンド・戦闘なし）で、「女将」1件は若女将という別人物。
説明された内容（女将・お茶・提灯・opening.mp4）は**すべて 248 側**にあった。「COMの館」は
「シオミの館」の音声入力誤記。

**構造的な原因**: この作品は3名義（フォルダ `248-shiomi-no-yakata` ／ リポジトリ・URL `248-day072` ／
タイトル `塩見の館`）を持ち、隣に `243-shiosai` `247-shiomiso` が並ぶ。AIは「243」と「妖怪館」という
両立しない2情報を渡され、毎回違う寄せ方をしていた。**SPEC 0章に別名表を置いて固定した。**

学び: **同じ指示で毎回ずれるときは、プロンプトより先に「指しているものが合っているか」を疑う。**

### 目的の穴を、既存資産で塞いだ
4ステージ案（館で倒す→館から逃げる→港→船）には論理の穴があった。**倒したのになぜ逃げるのか。**
「勝つ」がステージ1で完結してしまい、2以降の動機が無い。

直し方: **撃破をゴールではなく引き金にした。** 女将は主ではなく戸口の門番で、斬ると館が目を覚ます。
目的が「勝つ」から「帰る」へ反転する。貫く軸は**すでにゲーム内にあった「塩」**（祠の塩札）を使い、
4ステージ分の塩を集めて舳先から撒く＝脱出、とした。新しい概念を足さずに目的が通った。

学び: **目的の軸は、新規に発明するより既存の小道具を昇格させたほうが早い。** 塩は既に祠にあった。

### ゼロベースにしなかった判断の根拠
`ROOMS`（script.js）が `{id,kind:'talk'|'loot'|'play',bg,next}` の宣言的配列なので、
**ステージ追加はコードではなくデータ追加で足せる**。公開実体 1.07MB で 20MB 鉄則に18MB以上の余裕。
作り直すと、この土台と既存のピクセル素材14枚を捨てることになる。

### 自分で入れたバグを検証で1件捕まえた（会話幕を末尾に置くと落ちる）
`advance()`（会話送り）は無条件に `enterRoom(G.room.next,true)` を呼んでいた。終端判定
`next>=ROOMS.length → win()` は**戦闘パスにしか無かった**ため、新しく足した会話幕 `awake` を
末尾に置くと `ROOMS[11]` が undefined で `TypeError: Cannot read properties of undefined (reading 'place')`。

直し: 遷移を `goNext()` に一本化し、会話・拾い場・戦闘の3経路すべてが終端判定を通るようにした。

学び: **終端判定が1経路にしか無いのは、その経路でしか終わらせたことが無いから。**
新しい種類の部屋を末尾に置いた瞬間に露出する。ハーネス14項目は全部PASSしていて**これを検出していない**
（起動と描画しか見ないため）。変更を検証するテストは別途書く必要がある。

### 検証（実測）
Playwright/Chromium 390×844・hasTouch。script.js は IIFE で包まれているため、
検証用コピーの IIFE 内側にブリッジを挿して内部状態を読んだ（**公開ファイルは触っていない**）。

| 確認項目 | 結果 |
|---|---|
| 正体撃破 → 部屋が `awake` へ遷移 | PASS（mode=talk / room.id=awake） |
| 撃破直後の塩 | PASS（0 → 1） |
| HUD 表示 | PASS（`Lv1 脇差 / 旅装 / 塩 1/4`） |
| 全8行送り → クリア画面 | PASS（mode=clear・見出し「館が、目を覚ました」） |
| `tg.248.salt` の保存 | PASS（"1"） |
| タイトルの実績表示 | PASS（`第一幕 突破　塩 1/4　ベスト残りハート 3`） |
| pageerror / console error | 0件 |
| game-harness | 14項目 PASS（`docs/harness-reports/248-shiomi-no-yakata-2026-09-22T05-28-24-501Z.md`） |

**未検証**: iPhone実機。ステージ2〜4は未実装。公開（push）は未実施。


## 2026-09-21 OPからゲーム開始
- 従来はOP終了後に `title` へ戻るため、「はじめる」の後に「館へ入る」をもう一度押す必要があった。`endOp()` から `startAdventure()` を直接呼び、最初の会話へ自動移行するよう変更。
- `ended` だけに依存せず、再生失敗・動画エラー・スキップに加えて13秒の安全タイマーを設置。どの経路も `G.opDone` で一度だけ処理する。
- 動画を300msフェードアウトする間にゲーム画面を背面へ敷くため、黒画面を挟まない。

## 2026-09-20 BGM
- オープニング映像の音を19秒ループの `audio/inn.m4a`（196KB）にして、プレイ中に流す。ビープの3声はファイルが読めないときだけ。
- iOSは「はじめる」タップで Audio を一度 play→pause して許可を取り、OP後に本番再生。ミュートは `tg.248.mute`。
- 未検証: iPhone実機でのOP後再生。自動化では音の聴取確認はしていない。

## 2026-09-20 強くする／拾い場
- 要望は「強くしたい」「たまに経験値がすごい」「武器と装備の場所」。Lv1〜5、斬ダメージは武器＋レベル。金提灯が稀に出て経験値90。祠の塩札は経験値100付き。
- 場所は3つ足した。押入れ＝館の羽織（ハート4）、武具の間＝仕込み刀、祠＝塩札（無敵が長い）。再戦しても強さは残る。
- 未検証: 金提灯の出現は乱数なので、1回の通しでは出ないことがある。iPhone実機は未実施。

## 2026-09-20 プレイ画面のカメラと舞台
- 差の本体は絵が足りないことではなく、正方形に近い戦闘背景を縦画面に cover して屋根と空が7割を占め、キャラが下端に張り付いていたこと。廊下／中庭は床のある下半分を寄って描き、キャラをその床に立たせた。
- 会話は室内をビジュアルノベルのバストアップ、屋外は寄った舞台に立ち絵。台詞枠を青の汎用RPGから木材＋金縁へ。戦闘はヒットストップ、斬の残像、着地の埃、灯籠の光、木枠HUD。
- 帳場と中庭のキャプチャで、キャラが背景の床・縁側に乗っていることを確認。iPhone実機は未実施。

## 2026-09-20 プレイ画面をOP画風へ
- 落差の本体は、タイトル／OPがピクセルイラストなのにプレイが矩形の積み上げだったこと。背景4枚とキャラ／提灯／顔グラを同画風で足し、会話も戦闘も `drawImage` に切り替えた。
- 斬撃は専用スプライト＋火花。被弾で短い画面揺れ。部屋は帳場／客間／廊下／中庭で背景を分け、正体は中庭に赤みを重ねる。
- マゼンタ抜きは角からのflood fill。WebP化後の公開実体は約1MB台の見込み。

## 2026-09-19 初稿（塩見の館）
- オープニングは `opening.mp4`（元 3.4MB → 562KB）。WebKit は H.264 が黒画面になることがあるので、フレームが来るまでタイトル静止画を残し、届いたら映像を重ねる。
- 操作盤を先に置いた（75/25、Pointer Events、`setPointerCapture`、十字、斬／次へ、跳、ミュート、ポーズ）。`localStorage` は `tg.248.mute` / `tg.248.cleared` / `tg.248.best`。
- コアループは聞く・走る・斬る。帳場の茶を断ると提灯が1体増える。
- harness PASS: `docs/harness-reports/248-day072-2026-09-19T09-30-00-758Z.md`。通しプレイ（Chromium）で帳場→客間→廊下→中庭まで到達、コンソールエラー0。iPhone実機は未実施。

## 2026-09-19 公開
- URL: https://titan11111.github.io/248-day072/ （HTTP 200・Pages status=built・title=248｜塩見の館 を実測）
- commit: `3a1f525` add 248-day072 → `95173c1` add OGP tags
- 公開後 harness: `docs/harness-reports/248-day072-2026-09-19T09-31-17-206Z.md` → 14項目 PASS
- Slack: トークンなしのため未送信
- 未検証: iPhone実機

## 2026-09-21 フォルダ改名
- `248-day072` → `248-shiomi-no-yakata`（タイトル「塩見の館」をフォルダ名に反映）
- **GitHubリポジトリ名と公開URLは `248-day072` のまま据え置き**（タイタン判断）。理由: Pages はリポジトリ改名時にリダイレクトしないため、9/19公開の `https://titan11111.github.io/248-day072/` が404になる（鉄則8の事故パターン）
- 結果としてフォルダ名とリポジトリ名がズレている。`publish.sh <フォルダ名>` を使う場合は **リポジトリ名 `248-day072` を明示**すること
- `index.html` の `og:url`、過去の harness レポート名、公開URLの記述は**実測の事実なので書き換えない**
