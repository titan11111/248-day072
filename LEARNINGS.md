# 248-shiomi-no-yakata（旧 248-day072）LEARNINGS

## 2026-09-22 動画・音声を一旦オフ

- 動画: 前ターンで HTML/JS から OP 済み。mp4 実体はローカルに無し
- 音声: `USE_AUDIO=false`、`<audio>` 削除、`audio/inn.m4a` 削除。ミュートボタンは `body.no-audio` で非表示。WebAudio の SE/BGM コードは残し、フラグで無効化（復帰しやすくするため）
- 未検証: iPhone実機

## 2026-09-22 OP動画廃止・戦闘背景のスケール合わせ

### オープニング動画をやめ、画風は title.webp に寄せた
要望どおり `opening.mp4` の再生フロー（`op` 状態・video 要素・13秒タイマー）を削除。起動は boot → title（操作説明・ベスト）→ 館へ入る。キャンバス背面は従来 OP の poster と同じ `images/title.webp` のまま。

### プレイ中の背景をキャラと同じ倍率にした
差の本体は、背景だけ `cover` で画面いっぱいに引き伸ばし、キャラは `charH()`（spr-hero 420px 基準）で描いていたこと。`paintStageBG` で `scale = charH()/420` を背景矩形にも適用し、各 BG の床線（`BG_VIEW.*.floor`）を `gy()` に合わせた。帳場・客間・廊下・中庭すべてプレイ時に適用。会話は室内 cover のまま。

未検証: iPhone実機。`opening.mp4` はフォルダに残っていても HTML からは参照しない（サイズ削減は別途可）。

## 2026-09-22 目的の軸を入れ、女将撃破を「引き金」に変えた（ステージ1手目）

### 指示がずれ続けていた原因は、実装でもプロンプトでもなくフォルダ違いだった
タイタンから「243-shiosai-no-yoru の COMの館を作り直したい」と来たが、**243 に該当要素は1つも無い**。
実測: 243 の全ファイルを grep して「妖怪」0件 /「化け物」0件 /「お茶」0件、動画ファイル0個。
243 は離島の宿の**推理サウンドノベル**（5エンド・戦闘なし）で、「女将」1件は若女将という別人物。
説明された内容（女将・お茶・提灯・opening.mp4）は**すべて 248 側**にあった。「COMの館」は
「シオミの館」の音声入力誤記。

**構造的な原因**: この作品は3名義（フォルダ `248-shiomi-no-yakata` ／ リポジトリ・URL `248-day072` ／
タイトル `塩見の館`）を持ち、隣に `243-shiosai` `247-shiomiso` が並ぶ。AIは「243」と「妖怪館」という
両立しない2情報を渡され、毎回違う寄せ方をしていた。**SPEC 0章に別名表を置いて固定した。**

学び: **同じ指示で毎回ずれるときは、プロンプトより先に「指しているものが合っているか」を疑う。**

### 目的の穴を、既存資産で塞いだ
4ステージ案（館で倒す→館から逃げる→港→船）には論理の穴があった。**倒したのになぜ逃げるのか。**
「勝つ」がステージ1で完結してしまい、2以降の動機が無い。

直し方: **撃破をゴールではなく引き金にした。** 女将は主ではなく戸口の門番で、斬ると館が目を覚ます。
目的が「勝つ」から「帰る」へ反転する。貫く軸は**すでにゲーム内にあった「塩」**（祠の塩札）を使い、
4ステージ分の塩を集めて舳先から撒く＝脱出、とした。新しい概念を足さずに目的が通った。

学び: **目的の軸は、新規に発明するより既存の小道具を昇格させたほうが早い。** 塩は既に祠にあった。

### ゼロベースにしなかった判断の根拠
`ROOMS`（script.js）が `{id,kind:'talk'|'loot'|'play',bg,next}` の宣言的配列なので、
**ステージ追加はコードではなくデータ追加で足せる**。公開実体 1.07MB で 20MB 鉄則に18MB以上の余裕。
作り直すと、この土台と既存のピクセル素材14枚を捨てることになる。

### 自分で入れたバグを検証で1件捕まえた（会話幕を末尾に置くと落ちる）
`advance()`（会話送り）は無条件に `enterRoom(G.room.next,true)` を呼んでいた。終端判定
`next>=ROOMS.length → win()` は**戦闘パスにしか無かった**ため、新しく足した会話幕 `awake` を
末尾に置くと `ROOMS[11]` が undefined で `TypeError: Cannot read properties of undefined (reading 'place')`。

直し: 遷移を `goNext()` に一本化し、会話・拾い場・戦闘の3経路すべてが終端判定を通るようにした。

学び: **終端判定が1経路にしか無いのは、その経路でしか終わらせたことが無いから。**
新しい種類の部屋を末尾に置いた瞬間に露出する。ハーネス14項目は全部PASSしていて**これを検出していない**
（起動と描画しか見ないため）。変更を検証するテストは別途書く必要がある。

### 検証（実測）
Playwright/Chromium 390×844・hasTouch。script.js は IIFE で包まれているため、
検証用コピーの IIFE 内側にブリッジを挿して内部状態を読んだ（**公開ファイルは触っていない**）。

| 確認項目 | 結果 |
|---|---|
| 正体撃破 → 部屋が `awake` へ遷移 | PASS（mode=talk / room.id=awake） |
| 撃破直後の塩 | PASS（0 → 1） |
| HUD 表示 | PASS（`Lv1 脇差 / 旅装 / 塩 1/4`） |
| 全8行送り → クリア画面 | PASS（mode=clear・見出し「館が、目を覚ました」） |
| `tg.248.salt` の保存 | PASS（"1"） |
| タイトルの実績表示 | PASS（`第一幕 突破　塩 1/4　ベスト残りハート 3`） |
| pageerror / console error | 0件 |
| game-harness | 14項目 PASS（`docs/harness-reports/248-shiomi-no-yakata-2026-09-22T05-28-24-501Z.md`） |

**未検証**: iPhone実機。ステージ2〜4は未実装。公開（push）は未実施。


## 2026-09-21 OPからゲーム開始
- 従来はOP終了後に `title` へ戻るため、「はじめる」の後に「館へ入る」をもう一度押す必要があった。`endOp()` から `startAdventure()` を直接呼び、最初の会話へ自動移行するよう変更。
- `ended` だけに依存せず、再生失敗・動画エラー・スキップに加えて13秒の安全タイマーを設置。どの経路も `G.opDone` で一度だけ処理する。
- 動画を300msフェードアウトする間にゲーム画面を背面へ敷くため、黒画面を挟まない。

## 2026-09-20 BGM
- オープニング映像の音を19秒ループの `audio/inn.m4a`（196KB）にして、プレイ中に流す。ビープの3声はファイルが読めないときだけ。
- iOSは「はじめる」タップで Audio を一度 play→pause して許可を取り、OP後に本番再生。ミュートは `tg.248.mute`。
- 未検証: iPhone実機でのOP後再生。自動化では音の聴取確認はしていない。

## 2026-09-20 強くする／拾い場
- 要望は「強くしたい」「たまに経験値がすごい」「武器と装備の場所」。Lv1〜5、斬ダメージは武器＋レベル。金提灯が稀に出て経験値90。祠の塩札は経験値100付き。
- 場所は3つ足した。押入れ＝館の羽織（ハート4）、武具の間＝仕込み刀、祠＝塩札（無敵が長い）。再戦しても強さは残る。
- 未検証: 金提灯の出現は乱数なので、1回の通しでは出ないことがある。iPhone実機は未実施。

## 2026-09-20 プレイ画面のカメラと舞台
- 差の本体は絵が足りないことではなく、正方形に近い戦闘背景を縦画面に cover して屋根と空が7割を占め、キャラが下端に張り付いていたこと。廊下／中庭は床のある下半分を寄って描き、キャラをその床に立たせた。
- 会話は室内をビジュアルノベルのバストアップ、屋外は寄った舞台に立ち絵。台詞枠を青の汎用RPGから木材＋金縁へ。戦闘はヒットストップ、斬の残像、着地の埃、灯籠の光、木枠HUD。
- 帳場と中庭のキャプチャで、キャラが背景の床・縁側に乗っていることを確認。iPhone実機は未実施。

## 2026-09-20 プレイ画面をOP画風へ
- 落差の本体は、タイトル／OPがピクセルイラストなのにプレイが矩形の積み上げだったこと。背景4枚とキャラ／提灯／顔グラを同画風で足し、会話も戦闘も `drawImage` に切り替えた。
- 斬撃は専用スプライト＋火花。被弾で短い画面揺れ。部屋は帳場／客間／廊下／中庭で背景を分け、正体は中庭に赤みを重ねる。
- マゼンタ抜きは角からのflood fill。WebP化後の公開実体は約1MB台の見込み。

## 2026-09-19 初稿（塩見の館）
- オープニングは `opening.mp4`（元 3.4MB → 562KB）。WebKit は H.264 が黒画面になることがあるので、フレームが来るまでタイトル静止画を残し、届いたら映像を重ねる。
- 操作盤を先に置いた（75/25、Pointer Events、`setPointerCapture`、十字、斬／次へ、跳、ミュート、ポーズ）。`localStorage` は `tg.248.mute` / `tg.248.cleared` / `tg.248.best`。
- コアループは聞く・走る・斬る。帳場の茶を断ると提灯が1体増える。
- harness PASS: `docs/harness-reports/248-day072-2026-09-19T09-30-00-758Z.md`。通しプレイ（Chromium）で帳場→客間→廊下→中庭まで到達、コンソールエラー0。iPhone実機は未実施。

## 2026-09-19 公開
- URL: https://titan11111.github.io/248-day072/ （HTTP 200・Pages status=built・title=248｜塩見の館 を実測）
- commit: `3a1f525` add 248-day072 → `95173c1` add OGP tags
- 公開後 harness: `docs/harness-reports/248-day072-2026-09-19T09-31-17-206Z.md` → 14項目 PASS
- Slack: トークンなしのため未送信
- 未検証: iPhone実機

## 2026-09-21 フォルダ改名
- `248-day072` → `248-shiomi-no-yakata`（タイトル「塩見の館」をフォルダ名に反映）
- **GitHubリポジトリ名と公開URLは `248-day072` のまま据え置き**（タイタン判断）。理由: Pages はリポジトリ改名時にリダイレクトしないため、9/19公開の `https://titan11111.github.io/248-day072/` が404になる（鉄則8の事故パターン）
- 結果としてフォルダ名とリポジトリ名がズレている。`publish.sh <フォルダ名>` を使う場合は **リポジトリ名 `248-day072` を明示**すること
- `index.html` の `og:url`、過去の harness レポート名、公開URLの記述は**実測の事実なので書き換えない**

## 2026-09-22 — 操作盤をファミコン実機準拠へ全面改訂

### やったこと
`#control-deck` を「十字＋赤いact＋teal jump＋Ⅱ」の汎用パッドから、ファミコン実機の写しへ置き換えた。
エンジの本体、シャンパンゴールドのプレート、黒い十字キー、黒い窪みのSELECT・START、黒いハウジングの赤いA・B。

### 学び1: DOM順は「見た目」ではなく「検証器が最初に掴むもの」で決まる
最初は素直に **十字 → SELECT/START → A/B** の順で書いた。結果 harness が
`タップ: 失敗: locator.tap: Timeout 3000ms exceeded` で落ちた。

原因は `_tools/game-harness.mjs:188` の `page.locator('button, [role="button"], canvas').first()`。
**document 先頭の button をタップ対象にする**ため、先頭に来た `#btnMute`（`USE_AUDIO=false` で `disabled`）に
当たり、Playwright が「有効になるまで待つ」で 3秒タイムアウトしていた。

→ DOM順を **A/B → START/SELECT → 十字** にして、見た目は CSS の `row-reverse` で戻した。
これで先頭は常に有効な `#btnAct`（主操作）になる。**副産物として、検証器が押すのが主操作ボタンになった**＝検証の中身が良くなった。

**次に効く条件**: 操作盤の DOM を並べ替えるとき。先頭が「状況によって無効化されるボタン」だと、
見た目が正しくても「押せない操作盤」と判定される。

### 学び2: 実機の見た目と 48px 鉄則は、::before で両立できる
SELECT・START は実機では平たいゴム（高さ24px程度）。そのまま作ったら
`操作ボタン48px以上: btnMute:52×28, btnPause:52×28` で FAIL。

→ **button 本体を 52×48 の透明な当たり判定にして、赤いゴムは `::before` で中央に 24px 高で描いた。**
押下表現も `::before` を 2px 沈めるだけにして、ボタン自体は動かさない（当たり判定がぶれない）。

### 学び3: 「開けるが閉じられない」ポーズが、開発当初から残っていた
新コントローラの動作確認で発覚。`#btnResume` `#btnQuit` は **一度もバインドされていなかった**
（改修前の `script.js` を grep して 0件を確認済み。私の改修で壊したのではない）。

キーボードのある環境では Escape がダイアログを閉じるので気づかない。
**iPhone には Escape が無い**ので、Ⅱ を押したら二度と閉じられず、ゲームが固まっていた。

→ 2ボタンを配線し、さらに `close` / `cancel` イベントで `paused` を必ず降ろすようにした。

**次に効く条件**: `<dialog>` でポーズを作るとき。「開く」だけ配線して満足しない。
**閉じる経路を、キーボードの無い端末だけで一周させて確認する。**

### 検証（証跡）
- `_tools/game-harness.sh 248-shiomi-no-yakata` → **RESULT PASS（14項目全部）**
  レポート: `docs/harness-reports/248-shiomi-no-yakata-2026-09-22T11-04-57-931Z.md`
- WebKit・DPR2 で 320 / 375 / 390 / 430px を実測。
  3グループの**重なり 0px・プレートの横はみ出し 0px・48px未満のボタン 0個**
- 機能確認（WebKit）: A で boot→title→talk と遷移しラベルが「はじめる→館へ入る→次へ」と追従 /
  十字は押下で `press-r` が付き、離すと消える / START でポーズが開き、再開で閉じ、タイトルへで boot に戻る /
  コンソールエラー 0件・pageerror 0件

### 未検証（言い切らない）
- **iPhone 実機でのタップ感・親指の届き具合は未確認**（WebKit 自動検証のみ）
- 音を積み直したときの SELECT（ミュート）の実動作。いまは `USE_AUDIO=false` で `disabled` のまま

## 2026-09-22 — 戦闘画面の背景が画面を埋めていなかった（横幅を一切見ていなかった）

### 症状
タイタンから「画像が崩れている」。横長ウィンドウのスクリーンショットでは、部屋の背景が
右寄りの細い帯にしか描かれず、残りは黒。**調べたら iPhone 390px でも同じで、画面の44%が空白だった。**

### 原因: 倍率がキャラ身長だけで決まり、横幅を見ていなかった
`paintStageBG()`:
```js
const scale = charH()/BG_CHAR_PX;   // ← W が式に入っていない
const dw = sw*scale;
```
`charH()` は `min(H*0.32, W*0.50)`。背景素材は**全て540px幅**しかないため、
iPhone 390px で `dw = 540 × (195/420) = 251px` にしかならず、左右に各70pxの黒帯が出ていた。
横長 1300px では `dw = 215px` で、画面の76%が空白。

黒帯は事故ではなく **`fillRect` で明示的に塗られていた**（`'#120a18'`）ため、
「描けていない」ではなく「空白を描いている」状態で、エラーも警告も一切出ない。

### 直し方
1. `scale = Math.max(charH()/BG_CHAR_PX, W/sw)` — **横幅を埋めることを倍率の下限にする**
2. 上下の余りは平塗りをやめ、**素材の端の帯を引き伸ばして繋ぐ**
   （屋内の部屋の上に夜空が乗り、足元に黒い空白が残るのを解消）。
   切り出す帯は上5%・下1.5%。厚いと畳の目地ごと伸びて継ぎ目の線が出る
3. 横長ウィンドウでは `#screen-wrap` を `max-width: 56.25dvh` で**縦持ち比率に額装**。
   iPhone縦持ちでは 100% のほうが小さいので**実機の見え方は変わらない**

### 学び: harness は「絵が描かれているか」を1項目も見ていない
この不具合は harness 14項目を**全部PASSしたまま**存在していた。
存在・幅・Canvasサイズ・RAF・タップ・console・404・容量・レイアウト比率——
どれも「画面の44%が単色で塗られている」ことを検出できない。

→ **ピクセルを読む検査を別に書いた。** `getImageData` で塗りつぶし色（`#120a18` `#1a1018` `#3a2458`）の
占有率と、左右端の列が空白である割合を測る。

| 幅 | 修正前 空白率 / 左端 | 修正後 空白率 / 左端 |
|---|---|---|
| 320 | 46.8% / 93.4% | 2.1% / 0% |
| 390 | 43.9% / 93.1% | 3.3% / 0% |
| 430 | 44.0% / 93.4% | 3.4% / 0% |
| 1300(横長) | 76.2% / 89.3% | 2.1% / 0.4% |

**次に効く条件**: 背景をキャンバスに描く全ての作品。
「倍率を何か1つの基準（キャラ身長・高さ・幅）だけで決めていないか」を読む。
基準が1つなら、もう片方の軸は必ず余る。**余りを平塗りで隠すコードがあったら、それは仕様ではなく未解決の穴。**

### 検証（証跡）
- `_tools/game-harness.sh` → **RESULT PASS（14/14）**
  `docs/harness-reports/248-shiomi-no-yakata-2026-09-22T11-29-26-632Z.md`
- ピクセル検査（WebKit・DPR2・320/390/430/1300）→ **修正前 4件FAIL / 修正後 全件PASS**（上表）
- タイトル・会話・拾い場の各画面を4幅で撮影し目視。操作説明の A/B/十字/START 表記も表示を確認

### 未検証
- iPhone実機での見え方（WebKit自動検証のみ）
